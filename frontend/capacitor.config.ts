import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.coffeetalk.app',
  appName: 'coffeetalk-frontend',
  webDir: 'dist/coffeetalk-frontend'
};

export default config;
